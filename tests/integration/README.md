# Integration tests

User flows: lobby → play, guess flow, win/loss, i18n switch.

Runner: **Vitest** + **@playwright/test** (or Vitest browser mode when configured).

```bash
pnpm test:integration
```
