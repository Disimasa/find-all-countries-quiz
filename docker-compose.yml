services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ${SSL_CERT_DIR:-./ssl}:/etc/nginx/ssl:ro
    restart: unless-stopped
